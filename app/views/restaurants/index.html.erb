<center><div id="content">
<div class ="jumbotron">
  <h2>Shriek is the best way to find reviews on local restaurants.</h2>
  <h3>Explore the favorite diners, pastry shops, and eateries in your community.
  <br>Leave reviews and ratings for your fellow foodies.</h3>
</div>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Phone</th>
      <th>Website</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @restaurants.each do |restaurant| %>
      <tr>
        <td><%= restaurant.name %></td>
        <td><%= restaurant.address %></td>
        <td><%= restaurant.phone %></td>
        <td><%= link_to restaurant.website, restaurant.website %></td>
        <td><%= link_to 'Show', restaurant%></td>
        <td><%= link_to 'Edit', edit_restaurant_path(restaurant)%></td>
        <td><%= link_to 'Destroy', restaurant, method: :delete, data: { confirm: 'Are you sure?' }%></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<% if user_signed_in? %>
<div style="text-align:left">
<%= link_to 'New Restaurant', new_restaurant_path, class: "btn btn-link" %></div>
<% else %>
<div style="text-align:left">
<%= link_to "Sign In to Add", new_user_session_path %>
<!-- User must be signed in to add a new restaurant -->
</div>
<% end %>

<center>
<%= image_tag "/uploads/happy_footer.png" %></center></div></center><br>